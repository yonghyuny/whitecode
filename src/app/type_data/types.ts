import { ReactElement } from 'react';

// Month 드롭박스 타입정의
export type MonthProps = {
  value: number;
  label: string;
};

// Day 드롭박스 타입정의
export type DayProps = {
  value: number;
  label: string;
};
